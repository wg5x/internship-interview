# 面试题库

## 使用方式

不要机械地全部问。建议每位候选人选 1 个主项目深挖，再按其方向从浏览器自动化、通用 Agent 平台、RAG、工作流平台和工程落地中各抽 2-3 个问题。所有推进候选人都必须额外覆盖“Agent 思考、决策、反馈闭环”。

## 开场项目深挖

1. 选一个你最熟的浏览器自动化、Agent 平台、RAG 或工作流项目，画一下整体架构。
2. 这个项目里哪些模块是你独立设计和实现的？哪些是团队或开源方案已有的？
3. 你遇到过最难的问题是什么？当时有哪些备选方案？为什么选现在这个方案？
4. 简历里的指标是怎么测出来的？测试集多大？baseline 是什么？
5. 如果现在重新做，你会删掉或重构哪个模块？为什么？

## 浏览器自动运营 Agent 题

1. 如果要做“小红书/网页内容运营 Agent”，从登录、抓取、分析、生成、审核到发布，你会怎么拆任务流？
2. Playwright 和 Selenium 在动态页面、等待策略、稳定性、并发和调试上有什么差异？
3. 如何维护登录态、Cookie、localStorage、代理、设备指纹和账号风控边界？
4. 遇到验证码、滑块、反爬、请求限流或页面 DOM 频繁变化，你怎么设计降级和人工接管？
5. 自动发布内容前，如何做敏感词、重复内容、格式、图片、链接和权限校验？
6. 浏览器自动化任务失败后，哪些状态必须持久化，如何 resume？
7. 如何设计浏览器操作 trace，让面试官能复盘每一步点击、输入、截图和页面状态？
8. 如果一个任务要同时跑 50 个账号，如何控制并发、隔离状态、限速和异常封禁风险？

## 通用 Agent 平台 / Claw 类题

1. 类 Claw/WorkBuddy/龙虾的平台，你认为核心模块有哪些？Runtime、工具、上下文、记忆、审计如何分层？
2. Agent Runtime 主循环怎么设计？模型输出、工具解析、工具执行、observation 回填、final 输出之间如何流转？
3. 工具注册中心如何设计 schema、权限、超时、幂等、重试和风险等级？
4. checkpoint/resume 需要保存哪些状态？如何判断恢复时 workspace、memory、tool result 是否已经不一致？
5. 上下文工程怎么做？哪些信息进 prompt，哪些进 memory，哪些只进 trace？
6. 如何实现任务取消、follow-up 排队、同一会话串行化和多会话并发？
7. 如何防止 Agent 执行危险 shell、越权文件访问、重复工具调用或泄露敏感信息？
8. 如何设计离线 benchmark，验证一个通用 Agent 平台真的比 demo 稳定？

## RAG 题

1. 一个企业知识库 RAG，从文件上传到最终回答，完整链路有哪些步骤？
2. PDF、Word、网页、图片混合文档如何解析？什么时候需要 OCR 或 VLM？
3. chunk size、overlap、父子分块、语义分块分别解决什么问题？怎么选？
4. BM25、向量检索、混合检索、RRF、Cross-Encoder Rerank 的作用和代价是什么？
5. Query Rewrite、HyDE、Multi-Query 分别适合什么场景？有什么风险？
6. 如何判断 RAG 是召回问题、排序问题、上下文组装问题还是生成问题？
7. 如何设计 RAG 评测集？Hit@K、MRR、context precision、faithfulness 各看什么？
8. RAG 回答出现幻觉，你会怎么定位和修复？
9. 多轮对话里，历史消息和检索上下文如何拼接，避免 token 爆炸？
10. 如果用户问的问题命中了多个文档版本，如何处理版本冲突？

## Agent 与工作流题

1. ReAct、Plan-and-Execute、Reflection、Multi-Agent 分别适合什么任务？
2. 用 LangGraph 设计一个“问答 + 查数据库 + 建工单”的状态图，节点和边怎么定义？
3. 工具调用的 schema 应该怎么设计？如何处理参数缺失、参数错、工具超时？
4. 工具调用失败后，Agent 应该重试、换工具、问用户还是兜底？怎么判断？
5. 什么情况下需要多 Agent？多 Agent 相比单 Agent 的问题是什么？
6. Agent 的短期记忆、长期记忆、用户画像和任务状态有什么区别？
7. checkpoint/resume 在长任务里解决什么问题？哪些状态必须持久化？
8. 如何防止 Agent 调危险工具、越权访问、重复调用或泄露敏感信息？
9. 如何做 Agent 可观测性？你会记录哪些 trace 字段？
10. 如何评估一个 Agent 工作流的成功率？

## Agent 思考 / 决策 / 反馈专项

1. 选一个你做过的 Agent 项目，按 `plan -> route -> tool call -> observe -> evaluate -> retry/stop` 走一遍。
2. Agent 在什么情况下会选择检索、调用工具、追问用户、人工确认或直接回答？这个决策是谁做的？
3. 工具调用失败、参数错误、结果低置信度、重复调用时，系统如何决定重试、换工具、降级或停止？
4. 你们的 trace 里记录哪些字段，能否从一次线上失败回放到具体 plan、tool call、observation 和决策点？
5. 用户纠正 Agent 的判断后，这个反馈会进入 prompt、长期记忆、评测集、工具约束，还是只记录日志？
6. 如果 Agent 陷入循环调用，系统如何检测？阈值、状态、审计和人工接管怎么设计？
7. LLM-as-Judge、RAGAS、人工标注、规则校验分别适合评估哪类失败？怎么避免 Judge 自己不稳定？
8. 如何把失败样例沉淀成 benchmark，并确保后续 prompt、工具或模型调整没有回归？
9. 如果一个 Agent 需要跨天执行任务，哪些状态必须持久化？恢复时如何判断上下文、工具结果和外部页面已过期？
10. 如果只给你一周上线 MVP，你会保留哪些思考/决策/反馈能力，砍掉哪些？

## Coze / Dify / 工作流平台题

1. Coze/Dify 这类低代码平台适合做哪些节点？哪些逻辑你会坚持自研？
2. 一个运营 Agent 工作流里，意图识别、检索、内容生成、审核、发布、复盘分别怎么建节点？
3. 平台节点之间如何传递结构化状态？如何避免节点输出格式漂移？
4. 工作流失败时，如何做重试、跳过、人工确认和回滚？
5. Coze/Dify 插件或工具接入时，schema、鉴权、限流和错误码怎么设计？
6. 如何比较 LangGraph 自研工作流和 Coze/Dify 工作流的取舍？

## 工程落地题

1. FastAPI/SSE/WebSocket 在大模型流式输出里怎么选？
2. 一个 RAG/Agent 服务上线，你会怎么做 Docker 镜像、配置、日志和监控？
3. 向量库、关系库、Redis 分别存什么？哪些数据不能只放向量库？
4. 高并发下，embedding、rerank、LLM 调用哪个最容易成为瓶颈？怎么优化？
5. 如何做模型调用层的多 provider 路由、降级、重试和熔断？
6. 如何设计 Prompt Registry 或工具注册中心？
7. 如何做 API Key、用户数据和知识库的租户隔离？
8. 如果一次 Agent 任务有 30 次工具调用，怎么控制延迟、成本和失败率？
9. 你会给 RAG/Agent 项目写哪些测试？
10. 如何让面试官相信你的项目不是只调用框架跑 demo？

## 部署、训练、蒸馏、RL 后训练题

1. LoRA 和 QLoRA 的区别是什么？什么时候需要微调，什么时候只做 RAG？
2. SFT 数据怎么构造？如何清洗低质量样本？
3. 训练时你会关注哪些指标？loss 下降是否一定代表效果变好？
4. vLLM、Ollama、Transformers 本地推理各有什么取舍？
5. 模型量化会影响什么？如何评估精度和延迟变化？
6. 知识蒸馏解决什么问题？学生模型和教师模型怎么评估？
7. RLHF、DPO、GRPO 的基本区别是什么？
8. 医疗、金融等高风险场景做安全对齐，你会怎么设计 reward 或偏好数据？
9. 微调后如何避免模型遗忘通用能力？
10. 训练/部署成本受限时，你会怎么在 RAG、微调和提示词之间做取舍？

## 现场设计题

题目：设计一个“浏览器自动运营 Agent + 通用 Agent 工作台”。

候选人需要覆盖：

- 浏览器登录、抓取、表单操作、内容发布和人工接管。
- Playwright/Selenium 执行器、截图、DOM 解析、等待策略和异常恢复。
- 工具注册、权限控制、审计日志和风险操作审批。
- Agent Runtime、上下文工程、短期/长期记忆、思考/决策/反馈链路和 checkpoint/resume。
- RAG 素材库、历史案例库、运营知识库的检索、重排和引用。
- Coze/Dify 快速搭建和自研 LangGraph/Runtime 的边界。
- 评测指标、线上监控、成本和延迟控制。

追问：

1. 如果浏览器任务执行到一半页面结构变了怎么办？
2. 如果自动发布前发现内容命中敏感词或低置信度怎么办？
3. 如果同一账号连续失败 3 次，系统如何降级和保护账号？
4. 如果老板要求 1 周上线 MVP，你会砍掉哪些功能？
5. 如果线上成本或封号风险超预算，你先优化哪三个点？
6. 如果用户纠正了 Agent 的判断，这条反馈如何影响下一轮决策？
7. 如果 Agent 重复调用同一个工具，如何检测循环并让系统停下来？

## 简单实操题

1. 给一段用户问题，写出 Query Rewrite 的 prompt，并说明如何防止改写偏离原意。
2. 写一个工具 schema：`create_ticket(title, priority, assignee, description)`，并说明字段校验。
3. 设计 RAG trace 结构，至少包含 query、rewrite、retrieved docs、rerank score、answer、token cost。
4. 给一个低质量回答，要求候选人判断是召回、排序、上下文还是生成问题。
5. 让候选人用伪代码写一个 LangGraph 状态流：分类 -> 检索 -> 工具调用 -> 生成 -> 质检。
6. 写一个 Playwright 任务伪代码：打开页面 -> 登录态检查 -> 搜索关键词 -> 抽取内容 -> 保存截图和结构化结果。
7. 设计一个工具 schema：`publish_post(title, body, images, tags, account_id, dry_run)`，说明权限和人工确认。
8. 设计一个 Agent Runtime trace，至少包含 run_id、step、tool_call、browser_snapshot、approval、checkpoint、error。
9. 设计一个 Agent 反馈闭环数据结构，至少包含 plan、decision、observation、judge_result、user_feedback、next_action。
